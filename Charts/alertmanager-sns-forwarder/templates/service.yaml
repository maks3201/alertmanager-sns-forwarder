apiVersion: v1
kind: Service
metadata:
  name: {{ include "alertmanager-sns-forwarder.fullname" . }}
  labels:
    {{- include "alertmanager-sns-forwarder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alertmanager-sns-forwarder.selectorLabels" . | nindent 4 }}
